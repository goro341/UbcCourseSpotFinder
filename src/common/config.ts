// import * as dotEnv from 'dotenv';
// let dotEnvPath = './.env';
// // TODO: make this more elaborate once we have more parameters passed to the Node script
// dotEnvPath = process.argv[2] || dotEnvPath;
//
// // Load everything from .env into process.env
// dotEnv.config({path: dotEnvPath});
